    Secret Santa game

The above project is build using javascript language
In this project i using backend javascript framework nodejs

FrameWorks

   Nodejs :

	    This framework is used for backend for process the excel file and user data
               Here i am asking user to enter the file using cmd
	    For excel file reading i am using xlsx package which will help us to read data 
               From excel file , convert data into json and json to excel file.
	    In project i using process method then method to handle request 

    Execution command for nodejs backend:

	    To run node js we need npm which is javascript environment , where nodejs 
               Will run,npm is node package manager which will help us to manage different 
               external package in nodejs
	    Note :  For this you need to check nodejs is installed 
        if not install npm in Your  System
   
             For execution of code Run this code in terminal:
                    first go to current project path then in command type following commands
                          Command List:
                                                    npm install
                                                    npm run serve
Inputs:
	This project will take input as two file one is employee excel file and other is previous 
            Year excel result file

Project process:
	After taking input the files are send to node server which return json data of given file(employee list) and previous year result set
    ofter both data are used to generate new result set

Output:
	The output of given project is excel file which generate the result in the form of excel file which can find inside public folder in nodejs server
